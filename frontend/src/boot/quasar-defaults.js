import { QBtn, QTabs } from 'quasar'

QBtn.props.noCaps = {
  type: QBtn.props.noCaps,
  default: true
}

QTabs.props.activeBgColor = {
  type: QTabs.props.activeBgColor,
  default: 'teal-1'
}

QTabs.props.activeColor = {
  type: QTabs.props.activeColor,
  default: 'teal'
}

QTabs.props.narrowIndicator = {
  type: QTabs.props.narrowIndicator,
  default: true
}
